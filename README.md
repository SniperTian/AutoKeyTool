# AutoKey Pro (Windows版)

## 📌 简介
一个轻量级的 Windows 键盘自动化工具。支持可视化编辑按键序列、后台执行、全局热键控制。

## ⚙️ 核心功能
* **多线程执行**：界面不卡顿，支持毫秒级中断。
* **全局热键**：支持 F9/F10 等热键后台启动/停止。
* **按键序列**：支持 Ctrl+C, Alt+Tab 等组合键。
* **配置保存**：支持 JSON 格式导入导出。

## 🔨 开发与打包

### 1. 安装依赖
```bash
pip install -r requirements.txt